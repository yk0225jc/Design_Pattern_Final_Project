using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MushroomDecorator : WanderingShroom
{

    


    
    // Start is called before the first frame update
    void Start()
    {
        lt.color = (Color.red);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
